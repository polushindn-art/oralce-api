knife4j:
  enable: true
  setting:
    language: en
    enable-group: true
    enable-swagger-models: false
  documents:
    - group: main-api
      name: Документация
      locations: classpath:markdown/*
    - group: main-api
      name: Описание
      locations: classpath:markdown/description/*
    - group: main-api
      name: GitLab
      locations: classpath:markdown/gitlab/*
springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
    groups-order: asc
    try-it-out-enabled: true
  show-actuator: false
  group-configs:
    - group: oracle-packages
      display-name: Пакеты PKG_Oracle
      paths-to-match:
        - /mark/**
        - /tsdlist/**
        - /mail/**
        - /pictures/**
        - /public/**
        - /email/**
    - group: 1c-packages
      display-name: API 1C
      paths-to-match:
        - /api/1c/**
kkt:
  ip: 192.168.12.197     # IP адрес вашей ККТ Ритейл в локальной сети
  port: 7778             # Порт для сетевых команд (5557 для Ритейл/Штрих-М)